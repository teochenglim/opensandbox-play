[project]
name = "opensandbox-tutorial"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "opensandbox>=0.1.0",
    "opensandbox-mcp>=0.1.0",
    "rich>=13.0.0",
]

[tool.uv]
package = false
